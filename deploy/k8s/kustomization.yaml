apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secret.yaml
  - postgres.yaml # remove when using an external/managed Postgres
  - deployment.yaml
  - service.yaml
  - ingress.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "cityhall.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "cityhall.selector" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cityhall.selector" . | nindent 8 }}
    spec:
      {{- if .Values.workspaces.enabled }}
      serviceAccountName: {{ .Release.Name }}
      {{- end }}
      containers:
        - name: cityhall
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: BIND_ADDR
              value: "0.0.0.0:3000"
            {{- if .Values.workspaces.enabled }}
            - name: WORKSPACE_BACKEND
              value: kubernetes
            - name: WORKSPACE_K8S_VOLUME_SIZE
              value: {{ .Values.workspaces.volumeSize | quote }}
            {{- if .Values.workspaces.storageClass }}
            - name: WORKSPACE_K8S_STORAGE_CLASS
              value: {{ .Values.workspaces.storageClass | quote }}
            {{- end }}
            {{- end }}
            {{- range $k, $v := .Values.config.extraEnv }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "cityhall.secretName" . }}
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
